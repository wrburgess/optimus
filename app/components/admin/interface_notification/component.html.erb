<div class="container-fluid px-0 ms-2 mt-2 mb-2">
  <% flash.each do |type, message| %>
    <div class="alert alert-<%= type %> m-0 w-75">
      <strong>Notice: </strong><%= message %>
    </div>
  <% end %>
</div>
